{
  "name": "solarhisaab",
  "version": "1.0.0",
  "private": true,
  "description": "Understand your net-metering electricity bill, every month. Cloudflare Workers + D1 + R2.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply solarhisaab --local",
    "db:migrate:remote": "wrangler d1 migrations apply solarhisaab --remote",
    "check": "node --check public/app.js && node --check public/engine.js && node --check public/account.js && node --check public/admin.js && wrangler deploy --dry-run --outdir .wrangler/dry"
  },
  "devDependencies": {
    "wrangler": "^4.140.0"
  }
}
